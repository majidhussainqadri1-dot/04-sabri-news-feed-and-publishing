# File 04 — News Feed and Publishing — Legacy Foundation Adapter v2.0.7

File 04 remains a **temporary, write-disabled, auditable and reversible migration/compatibility adapter**. File 21 is the sole canonical publication/Home/News/feed owner; File 26 owns search/discovery; File 20 owns the shell; File 25 owns the visual system; File 24 coordinates assurance. File 04 does not create a second composer, feed, ranking service, comments/reactions store, moderation backend, search engine or permanent public route system.

## Governing scope

The source trace covers the consolidated governing plan, File 04 FR-001..013 and NFR-001..010, 71 CV requirements (all applicable central requirements), F04-CEN-01..02, 15 acceptance journeys and F04-FUT-001..018.

## v2.0.7 exact companion-contract parity

The fresh 20-pass cross-repository review found gaps that File 04's earlier source-only gates could not detect. v2.0.7 binds File 04 to corrected current companion contracts instead of self-declared hook names:

- File 00: immutable platform UUID plus a governed non-login unknown/deleted-author placeholder.
- File 21: package 1.0.6 / runtime API 1.0.4, consumes File 04 author/media context and supplies media preflight/post-migration verification.
- File 01: exact canonical route paths, no invalid page_id=0, and File 20's current system_recovery layout vocabulary.
- File 24: complete module assurance manifest fields using an accepted verification-level vocabulary.
- File 26: contract acceptance and completed canonical reindex evidence for cutover; accepted-but-unfinished search work is no longer enough.

These corrections do not change the File 04 storage schema, which remains 1.3.0. Companion source merge/CI, staging acceptance, live deployment and operational acceptance remain separate evidence states.

## v2.0.6 cross-file completion

The final independent R80 review corrected the remaining repository/source gaps instead of relying on the earlier merge-message claim. v2.0.6 adds:

- strict canonical File26 legacy-ID validation with no negative/malformed aliasing;
- explicit operator-authorized File01 module/route registration and a File20 route-context contract;
- File19 producer registration plus `LegacyMigrationBatchCompleted.v1`, `LegacyRecordQuarantined.v1`, `LegacyCutoverCompleted.v1` and `LegacyAdapterRetired.v1` event emission;
- File24 module assurance manifest/state integration while preserving native File04 controls;
- privileged read-only System Check/metrics access during a File21 outage without permitting migration writes;
- correction of the File04 plan REST namespace to `SNFLA_REST::NS`;
- strict retirement actor/lifecycle-version semantics;
- removal of completed one-time R80 helper workflow/scripts and permanent deterministic regression coverage.

Storage schema remains **1.3.0** because these corrections do not change custom-table structure.

## Truthful lifecycle status

Repository/source correction, deterministic packaging, exact-head CI, Hostinger staging acceptance, live deployment and sustained operations are separate states. This repository must not claim production readiness from source code alone. Real File00/File01/File19/File20/File21/File24/File25/File26 integration evidence, representative legacy data, browser/WCAG/RTL acceptance, backup/isolated restore, rollback/DR rehearsal, Founder approval and production monitoring remain external gates.
