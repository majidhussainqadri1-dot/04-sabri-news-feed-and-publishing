name: File04 eighty-round R34-R50 corrective self-patch

on:
  push:
    branches:
      - audit-file-04-eighty-round-hardening

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: audit-file-04-eighty-round-hardening
          persist-credentials: true
      - name: Apply and lint R34-R50 corrections
        id: patch
        shell: bash
        run: |
          set +e
          sed -i "s/'interaction_ledger' => \$progress ) ) {/'interaction_ledger' => \$progress ) ) ) {/" tools/apply-eighty-round-r34-r50.py
          python3 -m py_compile tools/apply-eighty-round-r34-r50.py
          python3 tools/apply-eighty-round-r34-r50.py > /tmp/r34-r50.log 2>&1
          patch_rc=$?
          cat /tmp/r34-r50.log
          lint_rc=0
          if [ "$patch_rc" -eq 0 ]; then
            while IFS= read -r -d '' file; do
              php -l "$file" >> /tmp/r34-r50.log 2>&1 || lint_rc=1
            done < <(find includes -name '*.php' -print0)
          fi
          if [ "$patch_rc" -ne 0 ] || [ "$lint_rc" -ne 0 ]; then
            cp /tmp/r34-r50.log EIGHTY-R34-R50-DIAGNOSTIC.txt
            git checkout -- includes
            echo 'failed=true' >> "$GITHUB_OUTPUT"
          else
            rm -f EIGHTY-R34-R50-DIAGNOSTIC.txt
            echo 'failed=false' >> "$GITHUB_OUTPUT"
          fi
          exit 0
      - name: Commit result
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if [ "${{ steps.patch.outputs.failed }}" = "true" ]; then
            git add EIGHTY-R34-R50-DIAGNOSTIC.txt
            git commit -m '80-round audit: record R34-R50 patch diagnostic'
          else
            git add -A includes EIGHTY-R34-R50-DIAGNOSTIC.txt
            git commit -m 'File 04 v2.0.4: correct defects from 80-round audit R34-R50'
          fi
          git push origin HEAD:audit-file-04-eighty-round-hardening
