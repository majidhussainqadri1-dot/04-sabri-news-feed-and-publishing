name: File04 round8 self patch
on:
  push:
    branches:
      - audit/file-04-second-ten-round-hardening
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: audit/file-04-second-ten-round-hardening
          fetch-depth: 0
      - shell: bash
        run: |
          if [ ! -f tools/round8-selfpatch.py ]; then exit 0; fi
          python3 tools/round8-selfpatch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Review 8 fix: close observability and provider evidence false-success paths"
          git push origin HEAD:audit/file-04-second-ten-round-hardening
